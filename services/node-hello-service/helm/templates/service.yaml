apiVersion: v1
kind: Service
metadata:
  name: {{ include "node-hello-service.fullname" . }}
  labels:
    {{- include "node-hello-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "node-hello-service.selectorLabels" . | nindent 4 }}
